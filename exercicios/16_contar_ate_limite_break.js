/**
 * Nível: Intermediário/Avançado
 * Tema: IF + break para encerrar cedo
 * Enunciado:
 *  - Imprima números de 1 até `n`, MAS se encontrar um número que seja múltiplo de 7, pare imediatamente (break).
 *  - Retorne a quantidade de números impressos.
 */
function contarAteLimite(n){
  // TODO: laço + if + break
}

console.log(contarAteLimite(5));  // 5  (1 2 3 4 5)
console.log(contarAteLimite(10)); // 6  (1 2 3 4 5 6 e para no 7)
