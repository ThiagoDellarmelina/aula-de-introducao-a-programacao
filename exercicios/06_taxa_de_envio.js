/**
 * Nível: Básico
 * Tema: IF com faixas
 * Enunciado:
 *  - Calcule a taxa de envio com base no valor da compra `total`:
 *      total < 50        => taxa = 20
 *      50 <= total < 100 => taxa = 10
 *      total >= 100      => taxa = 0
 *  - Retorne a taxa.
 */
function calcularTaxa(total){
  // TODO
}

console.log(calcularTaxa(30));   // 20
console.log(calcularTaxa(80));   // 10
console.log(calcularTaxa(120));  // 0
