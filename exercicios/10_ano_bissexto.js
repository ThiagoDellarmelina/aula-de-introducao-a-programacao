/**
 * Nível: Intermediário
 * Tema: IF com múltiplas condições
 * Enunciado:
 *  - Dado um ano inteiro `ano`, retorne true se for bissexto, senão false.
 *  - Regras: bissexto se (ano % 400 == 0) OU (ano % 4 == 0 E ano % 100 != 0).
 *  - Use apenas if/else (sem operadores lógicos encadeados no retorno).
 */
function ehBissexto(ano){
  // TODO
}

console.log(ehBissexto(2000)); // true
console.log(ehBissexto(1900)); // false
console.log(ehBissexto(2024)); // true
