/**
 * Nível: Intermediário
 * Tema: IF + early return (OBRIGATÓRIO usar return)
 * Enunciado:
 *  - Ajuste uma nota `n` para o intervalo [0,10].
 *    Se n < 0, retorne 0 imediatamente.
 *    Se n > 10, retorne 10 imediatamente.
 *    Caso contrário, retorne n.
 */
function normalizarNota(n){
  // TODO: use retornos imediatos (return) para os casos extremos
}

console.log(normalizarNota(-3)); // 0
console.log(normalizarNota(15)); // 10
console.log(normalizarNota(7));  // 7
