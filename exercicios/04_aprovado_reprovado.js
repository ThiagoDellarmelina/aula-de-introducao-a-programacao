/**
 * Nível: Iniciante
 * Tema: IF + validação de limites
 * Enunciado:
 *  - Receba uma nota (0 a 10) e retorne:
 *      "APROVADO" se nota >= 6
 *      "RECUPERAÇÃO" se 4 <= nota < 6
 *      "REPROVADO" se nota < 4
 *  - Valide a faixa (0..10): se for inválida, retorne "NOTA INVÁLIDA".
 */
function resultado(nota){
  // TODO
}

console.log(resultado(8));   // "APROVADO"
console.log(resultado(5));   // "RECUPERAÇÃO"
console.log(resultado(2));   // "REPROVADO"
console.log(resultado(12));  // "NOTA INVÁLIDA"
