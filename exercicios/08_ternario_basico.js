/**
 * Nível: Básico
 * Tema: Operador ternário (OBRIGATÓRIO)
 * Enunciado:
 *  - Receba um número inteiro `n` e retorne "PAR" ou "ÍMPAR" usando APENAS o operador ternário.
 *  - Proibido usar `if` aqui, para forçar o uso do ternário.
 */
function parOuImparTernario(n){
  // TODO: use (condicao ? valorSeVerdadeiro : valorSeFalso)
}

console.log(parOuImparTernario(4)); // "PAR"
console.log(parOuImparTernario(9)); // "ÍMPAR"
