/**
 * Nível: Avançado
 * Tema: Ternário (OBRIGATÓRIO)
 * Enunciado:
 *  - Dado o valor `v` e um cupom `cupom` ("NENHUM", "BRONZE", "PRATA", "OURO"),
 *    retorne a porcentagem de desconto:
 *      NENHUM => 0
 *      BRONZE => 5
 *      PRATA  => 10
 *      OURO   => 20
 *  - Use APENAS operador ternário (pode encadear). Não use if aqui.
 */
function desconto(cupom){
  // TODO: apenas ternário
}

console.log(desconto("NENHUM")); // 0
console.log(desconto("PRATA"));  // 10
console.log(desconto("OURO"));   // 20
