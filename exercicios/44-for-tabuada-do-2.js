/**
 * Nível: Super Iniciante
 * Tema: for
 * Enunciado:
 *  - Use um loop `for` para imprimir a tabuada do número 2 (até 10).
 */

for (let m = 1; m <= 10; m++) {
  console.log("2 x " + m + " = " + (2 * m));
}
