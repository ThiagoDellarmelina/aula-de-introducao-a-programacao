/**
 * Nível: Iniciante
 * Tema: IF básico
 * Enunciado:
 *  - Leia um número inteiro (variável `n`) e retorne "PAR" se for par, ou "ÍMPAR" se for ímpar.
 *  - Use apenas `if`/`else` (sem `switch`).
 *  - Não use operador ternário neste exercício.
 */
function parOuImpar(n){
  // TODO: escreva sua lógica usando if/else
}

console.log(parOuImpar(10)); // esperado: "PAR"
console.log(parOuImpar(7));  // esperado: "ÍMPAR"
