/**
 * Nível: Intermediário
 * Tema: IF + loop + break (OBRIGATÓRIO usar break)
 * Enunciado:
 *  - Dado um array `arr` e um limite `limite`, retorne o primeiro elemento > limite.
 *  - Se nenhum cumprir, retorne undefined.
 *  - Use `break` ao encontrar.
 */
function primeiroMaiorQue(arr, limite){
  // TODO
}

console.log(primeiroMaiorQue([1,4,6,2], 5)); // 6
console.log(primeiroMaiorQue([1,2,3], 10));  // undefined
