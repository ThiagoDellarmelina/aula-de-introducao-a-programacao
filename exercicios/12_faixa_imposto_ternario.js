/**
 * Nível: Intermediário
 * Tema: Ternário (OBRIGATÓRIO) com encadeamento
 * Enunciado:
 *  - Dado um salário `s`, retorne a faixa: "ISENTO", "BAIXA", "MEDIA", "ALTA".
 *      s < 2000 => "ISENTO"
 *      2000..4999 => "BAIXA"
 *      5000..9999 => "MEDIA"
 *      >= 10000 => "ALTA"
 *  - Use APENAS operador ternário (pode encadear).
 */
function faixaImposto(s){
  // TODO: usar apenas ternário
}

console.log(faixaImposto(1500)); // "ISENTO"
console.log(faixaImposto(3000)); // "BAIXA"
console.log(faixaImposto(7000)); // "MEDIA"
console.log(faixaImposto(12000)); // "ALTA"
