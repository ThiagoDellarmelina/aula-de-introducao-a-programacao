/**
 * Nível: Avançado
 * Tema: switch com return direto (OBRIGATÓRIO usar return)
 * Enunciado:
 *  - Comandos: "start","stop","status","help".
 *  - Retorne a mensagem correspondente diretamente em cada case (return).
 *  - Em default, retorne "comando inválido".
 *  - Proibido usar if/else.
 */
function cli(cmd){
  // TODO
}

console.log(cli("start"));  // "iniciando..."
console.log(cli("X"));      // "comando inválido"
