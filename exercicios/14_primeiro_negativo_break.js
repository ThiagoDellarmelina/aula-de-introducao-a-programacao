/**
 * Nível: Intermediário
 * Tema: IF + loop + break (OBRIGATÓRIO usar break)
 * Enunciado:
 *  - Dado um array de números `arr`, encontre e retorne o primeiro número negativo.
 *  - Se não houver, retorne null.
 *  - Use um loop e `break` quando encontrar o primeiro negativo.
 */
function primeiroNegativo(arr){
  // TODO: percorra o array com um loop, use if e break
}

console.log(primeiroNegativo([3,2,-5,4])); // -5
console.log(primeiroNegativo([1,2,3]));    // null
