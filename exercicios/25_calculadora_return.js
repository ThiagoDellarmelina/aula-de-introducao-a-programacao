/**
 * Nível: Básico
 * Tema: switch com return direto (OBRIGATÓRIO usar return)
 * Enunciado:
 *  - Implemente calc(a, b, op) com op em "+", "-", "*", "/".
 *  - Use switch e retorne diretamente em cada case (return).
 *  - Em default, retorne NaN.
 *  - Proibido usar if/else.
 */
function calc(a, b, op){
  // TODO: switch com returns diretos por operação.
}

console.log(calc(3,2,"+")); // 5
console.log(calc(6,0,"/")); // NaN (defina assim para divisão por zero)
