/**
 * Nível: Iniciante
 * Tema: IF encadeado
 * Enunciado:
 *  - Dada a idade (inteiro `idade`), retorne:
 *      "MENOR" se idade < 18
 *      "ADULTO" se 18 <= idade < 60
 *      "IDOSO" se idade >= 60
 *  - Use apenas `if` / `else if` / `else`.
 */
function classificarIdade(idade){
  // TODO
}

console.log(classificarIdade(12)); // "MENOR"
console.log(classificarIdade(30)); // "ADULTO"
console.log(classificarIdade(70)); // "IDOSO"
