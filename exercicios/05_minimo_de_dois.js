/**
 * Nível: Iniciante
 * Tema: IF para calcular mínimo
 * Enunciado:
 *  - Implemente `min2(a, b)` que retorna o menor dos dois números usando if (sem Math.min).
 */
function min2(a,b){
  // TODO
}

console.log(min2(3,7));  // 3
console.log(min2(9,-1)); // -1
