/**
 * Nível: Intermediário
 * Tema: fall-through intencional entre opções encadeadas
 * Enunciado:
 *  - Opções: "tudo", "sobre", "ajuda".
 *  - Se "tudo", deve executar "sobre" e depois "ajuda" via fall-through.
 *  - Use console.log; retorne a quantidade de linhas impressas.
 *  - Proibido usar if/else.
 */
function menu(op){
  // TODO: montar switch onde "tudo" cai em "sobre" e em seguida em "ajuda".
}

console.log(menu("tudo"));  // 2
console.log(menu("ajuda")); // 1
