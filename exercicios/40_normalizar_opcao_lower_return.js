/**
 * Nível: Avançado
 * Tema: `switch` com transformação + return
 * Enunciado:
 *  - Receba uma string `op` e use `toLowerCase()` para normalizar.
 *  - Trate cases: "on","off","auto"; retorne mensagens específicas via return direto.
 *  - Em default, retorne "opção inválida".
 *  - Proibido usar if/else.
 */
function modo(op){
  // TODO: usar op.toLowerCase() no switch e returns diretos.
}

console.log(modo("ON"));   // "ligado"
console.log(modo("Auto")); // "automático"
console.log(modo("X"));    // "opção inválida"
